import {services} from "@/lib/demo-data"; import Link from "next/link";
export default async function Service({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=services.find(x=>encodeURIComponent(x[0])===slug)||services[0];return <section className="container-x py-20"><span className="text-xs font-black text-gold">SERVICE</span><h1 className="mt-3 text-5xl font-black">{item[0]}</h1><p className="mt-6 max-w-3xl text-lg leading-10 text-muted">{item[1]}</p><div className="mt-10 rounded-2xl border bg-white p-8"><h2 className="text-2xl font-black">اطلاعات مهم</h2><p className="mt-3 leading-9 text-muted">شرایط و خدمات نهایی بسته به پرونده و مقصد متفاوت است. اطلاعات رسمی دانشگاه و مراجع مربوطه باید برای تصمیم نهایی بررسی شود.</p><Link href="/consultation" className="mt-6 inline-flex rounded-xl bg-royal px-6 py-3 font-bold text-white">دریافت مشاوره</Link></div></section>}