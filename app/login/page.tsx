"use client"; import {useState} from "react"; import {createClient} from "@/lib/supabase-browser"; import {useRouter} from "next/navigation";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setError("");const sb=createClient();const {error}=await sb.auth.signInWithPassword({email,password});if(error)setError("ورود انجام نشد. اطلاعات را بررسی کنید.");else router.push("/dashboard")}return <section className="container-x flex min-h-[650px] items-center justify-center py-20"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border bg-white p-8 shadow-premium"><h1 className="text-3xl font-black">ورود به حساب</h1><label className="mt-6 grid gap-2 text-sm font-bold">ایمیل<input className="rounded-xl border p-3" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="mt-4 grid gap-2 text-sm font-bold">رمز عبور<input className="rounded-xl border p-3" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button className="mt-6 w-full rounded-xl bg-royal py-3 font-bold text-white">ورود</button></form></section>}