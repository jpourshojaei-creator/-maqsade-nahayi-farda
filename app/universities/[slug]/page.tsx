import {demoUniversities} from "@/lib/demo-data"; import Link from "next/link"; import {notFound} from "next/navigation";
export default async function University({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const u=demoUniversities.find(x=>x.slug===slug);if(!u)notFound();return <section className="container-x py-20"><span className="text-xs font-black text-gold">{u.country} • {u.city}</span><h1 className="mt-3 text-5xl font-black">{u.name_fa}</h1><p className="mt-5 text-muted">{u.name_en}</p><div className="mt-10 grid gap-6 lg:grid-cols-3"><div className="lg:col-span-2 rounded-2xl border bg-white p-8"><h2 className="text-2xl font-black">معرفی</h2><p className="mt-4 leading-9 text-muted">{u.description}</p><h2 className="mt-10 text-2xl font-black">شرایط و مدارک</h2><p className="mt-4 leading-9 text-muted">این صفحه در نسخه متصل به Supabase اطلاعات برنامه‌ها، زبان، شهریه، مدارک، بورسیه، خوابگاه و FAQ را از دیتابیس نمایش می‌دهد.</p></div><aside className="rounded-2xl bg-navy p-7 text-white"><h3 className="text-xl font-black">بررسی شرایط پذیرش</h3><p className="mt-3 text-sm leading-7 text-slate-300">شرایط نهایی را برای پرونده خود بررسی کنید.</p><Link href="/consultation" className="mt-6 inline-flex w-full justify-center rounded-xl bg-royal px-5 py-3 font-bold">بررسی شرایط پذیرش</Link></aside></div></section>}